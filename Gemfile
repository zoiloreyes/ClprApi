source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in clpr-api.gemspec

gem "gfr_image_transformer", github: "ApexTech/GfrImageTransformer"
gemspec

group :development, :test do
  gem "awesome_print"
  gem "dotenv"
  gem "pry-byebug"
end
